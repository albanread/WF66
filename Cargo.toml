[package]
name = "wf64"
version = "0.1.0"
edition = "2021"
description = "A 64-bit STC Forth, JIT-compiled via the JASM assembler"
build = "build.rs"

[features]
default = []
gui_subsystem = []
# Optimizer measurement harness (bench/ corpus, static metric gate,
# opt-bench tool).  Pulls in a pure-Rust x86 decoder + hasher used ONLY
# to measure compiled-word codegen — kept out of the shipping binaries.
# Enable with `cargo test --features opt-metrics` / `cargo run --bin
# opt-bench --features opt-metrics`.
opt-metrics = ["dep:iced-x86", "dep:blake3", "dep:serde", "dep:serde_json"]

[dependencies]
# Path dep on JASM while both projects co-evolve. Flip to a git or
# crates.io dep once JASM stabilises.
wfasm = { path = "../JASM/rust", default-features = false }
newgc-core = { path = "../NewGC/crates/newgc-core" }
anyhow = "1"
# libloading — runtime DLL loading for factor.dll (NewFactor IDE).
libloading = "0.8"
# Optimizer measurement harness deps (feature = "opt-metrics" only).
# iced-x86: pure-Rust x86-64 decoder — classifies CALL/JMP/instructions in
# a compiled word body deterministically (never a raw 0xE8/0xE9 byte scan).
# blake3: body_hash / kernel_fingerprint / corpus_file_hash.
# serde(_json): the checked-in golden baseline files.
iced-x86   = { version = "1", optional = true, default-features = false, features = ["std", "decoder", "instr_info"] }
blake3     = { version = "1", optional = true }
serde      = { version = "1", optional = true, features = ["derive"] }
serde_json = { version = "1", optional = true }

[build-dependencies]
# Embeds the Win32 application manifest (DPI awareness, common
# controls v6, UTF-8 active code page) into wf64-ui.exe.
embed-resource = "3"

# iGui — Windows MDI front-end, ported from NewCormanLisp's iGui
# (which in turn borrows the renderer architecture from NewCP).
# Direct2D + DirectWrite + Direct3D 11 + the Win32 message pump.
# Linux/macOS get stub builds via cfg(windows) inside the module.
[target.'cfg(windows)'.dependencies]
windows-numerics = "0.3"
# docpane — the Markdown + Mermaid render core (forked from DocCrate),
# WF64's own copy under crates/.  Brings selkie (Mermaid parser) +
# pulldown-cmark.  Powers the in-window doc_pane / help_pane MDI
# children.  Kept as a local path dep so WF64 stays independent of
# NewFactor's tree.
docpane = { path = "crates/docpane" }

[target.'cfg(windows)'.dependencies.windows]
version = "0.62"
features = [
    "Win32_Foundation",
    "Win32_Graphics_Direct2D",
    "Win32_Graphics_Direct2D_Common",
    "Win32_Graphics_Direct3D",
    "Win32_Graphics_Direct3D11",
    "Win32_Graphics_DirectWrite",
    "Win32_Graphics_Dxgi",
    "Win32_Graphics_Dxgi_Common",
    "Win32_Graphics_Gdi",
    "Win32_System_Com",
    "Win32_System_LibraryLoader",
    "Win32_System_Performance",
    "Win32_System_Threading",
    "Win32_UI_HiDpi",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_Controls",
    "Win32_UI_Controls_Dialogs",
    "Win32_System_DataExchange",
    "Win32_System_Memory",
    "Win32_System_Ole",
    # VEH + EXCEPTION_POINTERS / EXCEPTION_RECORD for crash_handler (Phase 3b).
    # AddVectoredExceptionHandler is gated on Win32_System_Kernel.
    "Win32_System_Diagnostics_Debug",
    "Win32_System_Kernel",
]

[lib]
name = "wf64"
path = "src/lib.rs"

[[bin]]
name = "wf64"
path = "src/main.rs"

[[bin]]
name = "wf64-ui"
path = "src/bin/wf64_ui.rs"

[[bin]]
name = "port-wf32"
path = "src/bin/port_wf32.rs"

# Optimizer before/after measurement + bless tool. Feature-gated so the
# decoder/hasher deps never enter the default build graph.
[[bin]]
name = "opt-bench"
path = "src/bin/opt_bench.rs"
required-features = ["opt-metrics"]

[[bin]]
name = "golden-capture"
path = "src/bin/golden_capture.rs"
required-features = ["opt-metrics"]

[[bin]]
name = "rasm-diff"
path = "src/bin/rasm_diff.rs"
required-features = ["opt-metrics"]

[[bin]]
name = "newfactor-ui"
path = "src/bin/newfactor_ui.rs"

[profile.dev]
debug = 1

[profile.release]
lto = "thin"

# `release-ship` is the profile used by the packaging script
# (tools/build-release.ps1).  Builds the smallest, fastest binary
# we can ship: full LTO, single codegen unit, stripped symbols,
# abort-on-panic.  Slower to build than plain release; we only
# use it when assembling release/wf64/.
[profile.release-ship]
inherits = "release"
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
